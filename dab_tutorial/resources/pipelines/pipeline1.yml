resources:
  pipelines:
    pipeline_pipeline1:
      name: pipeline1
      libraries:
        - glob:
            include: ../../src/pipeline1/transformations/**
      schema: default
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: workspace
      serverless: true
      root_path: ../../src/pipeline1